# Artefactos generados — se regeneran ejecutando los scripts del pipeline
*.db
*.db-journal
*.db-wal
*.db-shm

# Datos fuente (CSV pesados, no se versionan)
data/ingesta1/*.csv
data/ingesta2/*.csv

# CSV exportados para Power BI (se regeneran con export_to_csv.py)
exports/*.csv
